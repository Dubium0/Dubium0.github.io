// src/components/mod.rs

// This file declares the modules within the `components` directory,
// making them accessible to other parts of the application (like main.rs).

// Declare each component file as a public module
pub mod about;
pub mod contact;
pub mod footer;
pub mod hero;
pub mod projects;
pub mod project_card;
pub mod navbar; // <-- Add this line

// Optional: You could also re-export the components here for easier imports
// ...
